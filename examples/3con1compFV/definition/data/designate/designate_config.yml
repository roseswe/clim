configuration-data:
- data:
    dns_domain: example.org.
    ns_records:
    - hostname: ns1.example.org.
      priority: 1
    - hostname: ns2.example.org.
      priority: 2
  name: DESIGNATE-CONFIG-CP1
  services:
  - designate
product:
  version: 2
